CREATE DATABASE rotten;

USE rotten;

CREATE TABLE rottus (
    id INT(20) NOT NULL  PRIMARY KEY,
    nameus VARCHAR(20),
    lastn VARCHAR(20),
    email VARCHAR(30),
    pass VARCHAR(80),
   
);


DESCRIBE rottus;
